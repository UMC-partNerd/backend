spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
  r2dbc:
    url: r2dbc:h2:mem:///testdb
    username: sa
    password:
  profiles:
    active: test
